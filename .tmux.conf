############### MAKE TMUX USEFUL (Kyle's config) ###############
# Enable mouse mode for tmux, jk trying to learn keebs so hardmode, no mouse
# set -g mouse on 

# address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Set the proper terminal type/upgrade $TERM
set -g default-terminal 'screen-256color'

# Set true colors so 'termguicolors' works properly for neovim
set-option -sa terminal-overrides ',xterm-256color:RGB'

# Increase the scrollback limit
set -g history-limit 200000

# tmux messages are displayed for 4 seconds
set -g display-time 4000

# refresh 'status-left' and 'status-right' more often
set -g status-interval 5

# focus events enabled for terminals that support them
set -g focus-events on

# Enable clipboard copying on selection
set-option -s set-clipboard on

# Easier & faster switching between next/prev window
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# source .tmux.conf as suggested in `man tmux`
bind R source-file ~/.tmux.conf \; display "tmux config reloaded!"

# set only on OS X where it's required
set -g default-command "reattach-to-user-namespace -l $SHELL"

# Spotify Status Bar Addon (download bin from here: https://github.com/jdxcode/tmux-spotify-info)
set -ga status-right '#(tmux-spotify-info)'

###############        TMUX PLUGIN MANAGER       ###############
# List of plugins
set -g @plugin 'nhdaly/tmux-better-mouse-mode' # convenient while still learning, @FUTURE: remove... mouse is bad ;)
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'xamut/tmux-spotify'
set -g @plugin 'kylepeeler/tmux-dracula'
set -g @plugin 'jbnicolai/tmux-fpp'
# set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'
# set -g @continuum-restore 'on'


# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of .tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'
